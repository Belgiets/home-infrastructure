PORT=3001

# Backend API URL (local dev — direct to backend, bypasses proxy)
NEXT_PUBLIC_API_URL=http://localhost:3000

# Backend URL for Vercel proxy rewrites (server-side only, set in Vercel dashboard)
# BACKEND_URL=https://dashboard-backend-dev-xxxxx-ew.a.run.app
